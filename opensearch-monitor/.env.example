PORT=3000

# Base URL of the OpenSearch cluster to monitor.
OPENSEARCH_URL=http://localhost:9200

# Optional basic auth, only sent if OPENSEARCH_USER is set.
# OPENSEARCH_USER=admin
# OPENSEARCH_PASSWORD=changeme
