kockpit:
  audit:
    local:
      storage-path: ${user.home}/IdeaProjects/kockpit/data/manifests
  sdk:
    filesystem:
      local_directory: ${DATA_MANIFESTS:/kockpit/data/manifests}

    manifest:
      filesystem:
        path: ${user.home}/IdeaProjects/kockpit/data/manifests
